{
  "mcpServers": {
    "nl": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "git+https://github.com/rubatoyd/nl-openapi-mcp", "nl-mcp"],
      "env": {
        "NL_API_KEY": "${NL_API_KEY:-}",
        "NL_OS_TRUST": "${NL_OS_TRUST:-1}"
      }
    }
  }
}
